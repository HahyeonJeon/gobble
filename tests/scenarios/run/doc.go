// Package run owns evidence that an operator can execute each selected primary
// path in a caller-owned workspace and receive all required declared assay
// artifacts, reports, state, and provenance. A failed task cannot produce a
// false complete product. It does not own an assay graph or fixture.
package run
