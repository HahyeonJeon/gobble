// Package design owns evidence that an author can find a named product, its
// typed samples, defaults, selected stages, outputs, and assay testdata owner.
// It verifies that only a pipeline package authors an assay graph and that an
// unsupported route or input has a named structured boundary. It does not own
// product graphs or fixtures.
package design
