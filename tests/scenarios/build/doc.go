// Package build owns evidence that typed samples and Config compose into the
// same validated, inspectable graph through Build and the default Pipeline
// adapter. Missing references, invalid sheets, paths, and option conflicts
// must be structured compose defects before workspace mutation. It does not
// own an assay graph or fixture.
package build
