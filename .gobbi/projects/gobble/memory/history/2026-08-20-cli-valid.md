# CLI live validation completed

**Completed at:** 2026-08-20T12:43:26Z

## Changes

- Added `tests/cli-valid` live CLI proofs for run-local recover, WGS recover, RNA-seq run+inspect, and Methyl-seq run+inspect, plus a hermetic workflow-case plan golden. Product CLI at `cmd/gobble` is unchanged.
- Commits oldest first: `c688b9b` wrappers; `b27c7a0` TMPDIR isolation; `2065d7e` run-local live recover; `68f6f13` WGS live recover; `44040db` RNA-seq and Methyl-seq live run. Work started from `develop` `d749353`.
- Removed backlog `CLI live and WGS first-horizon proof`. Temp driver cleanup on interrupt during compile remains deferred. First-horizon exit is not claimed.
- Current design is in [Design Memory](../design/README.md). The session note is [CLI live validation](../reports/note/2026-08-20-cli-valid.md).
