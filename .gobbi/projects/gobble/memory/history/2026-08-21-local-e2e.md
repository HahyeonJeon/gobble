# Local samplesheet e2e pack completed

**Completed at:** 2026-08-21T00:25:00Z

## Changes

- Added a public CSV samplesheet API in package `gobble` and CLI `--sample PATH` on compose, validate, plan, run, and resume. Default omitted path is process-cwd `samplesheet.csv`, not `--workspace`. inspect and release reject the flag. Sheet errors exit 2.
- Replaced `assets.RNASeq()` and `assets.MethylSeq()` with samplesheet multi-sample graphs. RNA runs four samples, two groups, featureCounts, merge counts, and one two-group DESeq2 contrast. Methyl runs two samples through extract and merged MultiQC. DMR is deferred. `WGS()` stays authored modules.
- Moved scenario live proofs to `tests/local-e2e` and deleted replaced copies after a parity map. Recover occupy/remaining/release/resume is proved on run-local, WGS, RNA, and Methyl through API and CLI. Live RNA uses distinct GSE110004 pairs SRR6357070–SRR6357073 because reused FASTQs produced identical counts.
- Commits oldest first: `c67a88c` samplesheet API; `74732d6` DEG adders; `3c2bfbf` `--sample`; `39b8fdf` constructors; `f231330` live pack. Work started from `develop` `990b4c5`. First-horizon exit is not claimed.
- Current design is in [Design Memory](../design/README.md). The session note is [Local samplesheet e2e pack](../reports/note/2026-08-21-local-e2e.md).
