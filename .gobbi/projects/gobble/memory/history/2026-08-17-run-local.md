# Local Run shipped

**Completed at:** 2026-08-17T16:50:25Z

## Changes

- Shipped public `Run(graph, workspace, cap)` on `feat/run-local` in module `github.com/HahyeonJeon/gobble`. After checks, Run occupies the caller workspace under `.gobble/`, isolates tasks, stages plan-relative paths, runs process and Docker tasks, and persists state.
- Session proof is the synthetic fixture `testdata/run-local/` (`alpine:3.21` plus process). First-check remains `go test ./...`. Live Docker tests skip if the daemon is down. The workflow-case golden `testdata/workflow-case/plan.json` is unchanged.
- There is no CLI, inspect, or resume. A second start on the same workspace is an occupied error. Current design position is in [Design Memory](../design/README.md). The session note is [Local Run session](../reports/note/2026-08-17-run-local.md).
