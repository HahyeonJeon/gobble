# CLI live validation session

The session added live-tagged CLI proofs at `tests/cli-valid` without changing product CLI source.

Result: five `Pipeline()` wrappers for run-local, WGS, RNA-seq, Methyl-seq, and workflow-case. Hermetic `TestWorkflowCaseCLIPlan` execs a module-root built `gobble` and matches `testdata/workflow-case/plan.json`. Live-tagged tests prove run-local recover (`TestRunLocalCLIRecover`), WGS recover (`TestWGSCLIRecover`), RNA-seq run+inspect (`TestRNASeqCLIRun`), and Methyl-seq run+inspect (`TestMethylSeqCLIRun`). `gobbleCommand` sets child `TMPDIR` to a test-owned dir so `cmd/gobble` `watchDriverTemps` does not fail under package-parallel `go test`. Pin cache under `tests/cli-valid/testdata/cache/` is ignored.

Evidence: work HEAD `44040db` from `develop` `d749353`. Commits oldest first: `c688b9b`, `b27c7a0`, `2065d7e`, `68f6f13`, `44040db`. Combined diff is `tests/cli-valid/` only. Hermetic `go test ./...` PASS. Live tests PASS with `-tags=live`. Closure eval PASS. Publication is local.

Limits: no product CLI change; first-horizon Memory close is not claimed; interrupting a graph verb during compile can still leave `/tmp/gobble-driver-*`.

See [system.md](../../design/architecture/system.md) Verification, [run-local.md](../../design/feature/run-local.md) Interfaces Current, and [history](../../history/2026-08-20-cli-valid.md).
