# Gobble v0.1.0 release

The first public pre-1.0 tag is `v0.1.0` for module `github.com/HahyeonJeon/gobble`. Agents install that exact tag for the library and `cmd/gobble`. Humans still receive one packed `linux/amd64` runner. Local path pin remains valid for unpublished trees. MIT LICENSE is unchanged. `@latest` is unsupported.

Evidence: public text and Design Current name `v0.1.0`. Release notes are `CHANGELOG.md`. Hermetic first check is `go test ./...`. First-horizon local-pin and packed evidence remains `go test -tags=live ./tests/install-e2e`.

Limits: no GitHub Release binary artifacts; no retag; linux/amd64 and Go 1.26 only.

See [Design Memory](../../design/README.md) and [history](../../history/2026-08-30-gobble-v0.1.0.md).
