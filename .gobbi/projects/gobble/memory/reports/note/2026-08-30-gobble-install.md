# Gobble install session

The session made Gobble user-installable as a dual-family first-horizon product. Agents pin a matched Go CLI and library. Humans receive one packed linux/amd64 trampoline.

Result: Occupy identity is bound. Local-pin occupy uses git of the selected module directory and ignores a linked-worktree `vcs.revision` stamp. A proved-stopped Docker task may keep a RuntimeID for log-copy or `docker rm` retry without becoming `unknown-backend` or wedging occupancy. `gobble pack [package] --output PATH` writes one linux/amd64 trampoline that needs no Go at run time. The tree grants MIT under `Copyright (c) 2026 HahyeonJeon`. Packed help embeds that notice and scopes MIT to Gobble portions; the embedded pipeline's author owns its license. First-horizon installed-path exit is proved for the external API, GOBIN CLI, and packed WGS remaining-work path.

Evidence: work started from `develop` `e2df003b55a3c3285b14f037c41e420ec3187a40`. Accepted heads oldest first: `7fbd5254e08e3896b92bc3bf09541710e165ad3d` occupy identity and leftover Docker; `0794467472ee484224f4d444e70ec2e17f8ba294` pack trampoline; `ae127d75551ef6b0237cf7c65d6b2e9668c76e40` live assay, MIT, and Design Current. Hermetic `go test ./...` PASS at `ae127d7`. Live `go test -tags=live ./tests/install-e2e` PASS. Publication is local. Merge is not this Memory write.

Limits: no tag, push, or GitHub Release; first `v0.x.y` unnamed; exact-tag `@v0.x.y` unproved; leftover scatter-expand worktree is not this session.

See [Design Memory](../../design/README.md), [history](../../history/2026-08-30-gobble-install.md), [product Current](../../design/process/product.md), and [recover-run Current](../../design/feature/recover-run.md).
