# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/

# Official local-e2e fixture cache (dataset bytes stay untracked)
tests/local-e2e/testdata/cache/

# Omitted --sample CLI cwd (created under the module so go list finds go.mod)
/local-e2e-cwd-*/

# First-party asset proof cache (dataset bytes stay untracked)
assets/testdata/cache/
