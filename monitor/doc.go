// Package monitor reads coherent Gobble execution snapshots and projects them
// into stage graphs, sample progress, and attention lists. It has no terminal
// dependency and never mutates a workspace. The terminal frontend is in tui.
package monitor
